# 项目验证策略

状态：候选，待项目负责人确认或映射现有规则来源。

> 已有等效规则来源则复用，不创建竞争文件。以下待填写项不构成已确认策略。

## 验证对象与边界

关键用户流程：[待填写]。主要风险：[待填写]。
必需覆盖的依据：[引用现行需求、规则或接口契约；不能仅从已有实现或测试清单反推完整性]。
程序可以证明：[待填写]。实际环境证据要求：[待填写]。
必须由人判断的质量 / 产品结果：[待填写或不适用]。

## 范围、命令与升级

| 变更或风险 | 定向检查的确切命令 | 受影响消费者 / 流程命令 | 环境与触发 |
|---|---|---|---|
| [源码或行为] | [待填写] | [待填写] | [待填写] |
| [生成源或公共接口] | [待填写] | [待填写] | [待填写] |
| [文档或输入规约] | [待填写] | [待填写] | [待填写] |

选择前检查实际变更、导入、消费者、测试映射和CI。多个变化取所需检查并集。
未知路径或删测试先核对消费者与映射，不静默通过，不因不熟悉而自动全量。
全量仅在跨模块公共契约变化、依赖面经检查仍不能界定、正式发布或结项明确要求，或项目现行策略另有要求时选择。局部公共契约变化先核对实际消费者；记录具体触发及确切命令。
执行前声明待验证结论、受影响面、级别、命令、升级条件；跑所选命令到结束。

测试集维护按项目映射的verification.md“测试集维护”；本项目的关键回归、契约/历史数据夹具及相关维护约定：[复用已有位置，按风险填写]。适用安全检查的命令、对象、漏洞信息新鲜度与隔离要求纳入上表，安全维度沿security-review.md，不重复清单。

性能、容量或运行费用相关时，复用项目现行要求记录适用规模、指标/预算及代表检查入口：[按需填写或引用]；触发、比例与结论边界沿项目映射的verification.md“性能、容量与运行费用”，没有相关风险不新增必填项。

## 环境与执行时机

工具、服务、外部依赖、版本及隔离数据：[待填写]。
离线 / 假端点适用范围：[待填写]。真实入口 / 真实模型探针条件：[待填写]。
每次修改、阶段、集成、发布分别何时运行：[待填写，按风险选择]。
关键检查器用已知错误与合法变体校准；测试辅助逻辑不能代替所声称验证的生产行为，实际用户结果不由变量、函数存在或HTTP200代替。
接入项目执行机制时，按风险选择其控制案例：缺验收、漏必需检查、全跳过、相关输入变化、无效执行回执、事件失效和中断后的开发任务恢复，以及合法简写与无关变化。只有实际执行过的案例可写通过；没有执行器时不得声称强制阻断已验证。
机制自身校准在首次接入、相关执行器或映射和交付检查逻辑变化、控制漏检或误拦时启动，重验受影响案例，不默认每个功能任务重跑所有机制检查。普通功能的验证随任务节点由开发 AI 主动选择执行，不需要用户另发跑测试命令。

## 证据、跳过与失效

完整命令、输出、退出码、跳过、相关源码和输入、环境、时间与证据路径必须记录。长检查逐项保存，终端丢失先读结果。
失败按证据区分本次回归、既有缺陷、测试问题或环境问题；未定位时保留未知，既有失败不自动免除当前必需验收。
必需检查全部跳过或环境缺失标未验证；部分跳过列出未覆盖结果，不能声称对应能力通过。
输入规约、相关实现、测试、配置或环境变化使受影响旧证据失效，复验受影响范围。提交 / 服务验收核对实际受验版本。
证据可复用条件、项目特定新鲜度期限：[待填写]。
原始证据不得覆盖；本机证据保存与备份位置：[待填写]。正式发布证据的持久位置：[待填写或不适用]。
日志脱敏、访问、保留与清理规则：[待填写]；证据不可读时不得继续显示为可用。

## 人工验收与策略维护

负责人工验收的人、材料、标准与确认记录位置：[待填写或不适用]。
自动通过不等于人工验收或发布授权。模型质量比较区分开发与独立样本；主观评价需适用的人工校准。
覆盖、风险、命令、环境、失效或检查含义变化时更新本验证策略，最晚在相关检查执行前；不得降低必需检查以取得绿色结果。
